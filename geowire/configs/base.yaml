seed: 3407

paths:
  project_root: .
  data_root: data
  weight_root: models
  cache_root: cache/georoute

runtime:
  output_dir: runs/debug
  device: cuda
  precision: bf16

qwen:
  checkpoint: Qwen/Qwen3-VL-2B-Instruct
  local_path: models/Qwen3-VL-2B-Instruct
  input_mode: ordered_images
  min_pixels: 262144
  max_pixels: 1048576

geometry:
  vggt_checkpoint: facebook/VGGT-1B
  vggt_local_path: models/VGGT-1B
  vggt_preprocess_mode: pad
  dtype: bfloat16

graph:
  topk_cross_frame: 4
  self_loop_weight: 1.0
  visibility_min: 0.50
  track_conf_min: 0.50
  depth_conf_min: 1.00
  reproj_error_px_max: 8.0
  depth_rel_error_max: 0.08
  cycle_error_px_max: 6.0
  tau_reproj: 4.0
  tau_cycle: 4.0
